service:
  name: sensors-are-down-alexa-skill

frameworkVersion: ">=1.4.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10

functions:
  sensorsAreDown:
    handler: index.handler
    events:
      - alexaSkill
